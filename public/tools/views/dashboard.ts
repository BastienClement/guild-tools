import { Element, Property, Listener, Dependencies, Inject, On, Watch, Bind, PolymerElement } from "elements/polymer";
import { View } from "client/router";

@View("dashboard", "gt-dashboard", "/dashboard")
export class Dashboard extends PolymerElement {

}
